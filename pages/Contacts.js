import ContactsList from "./components/lists/ContactsList"

const Contacts = () => {
    return (
        <div className="section ">
        <ContactsList/>
        </div>
    );
}

export default Contacts;